TrendSnap monorepo skeleton. Contains `frontend/`, `backend/`, and `scrapers/` directories.

Start front-end:

```bash
cd frontend
npm install
npm run dev
```

Start backend (dev):

```bash
cd backend
npm install
npm run dev
```
